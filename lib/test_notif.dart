import 'package:flutter_local_notifications/flutter_local_notifications.dart';

void main() async {
  final p = FlutterLocalNotificationsPlugin();
  
  // Test 1: no args
  // p.initialize();
  
  // Test 2: InitializationSettings
  // p.initialize(InitializationSettings());
}
